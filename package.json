{
  "name": "@defra/lis-sdk-npm-api-feature-flags",
  "version": "0.0.1",
  "description": "JavaScript/Node client SDK for the Livestock feature flag API.",
  "main": "src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/DEFRA/lis-sdk-npm-api-feature-flags.git"
  },
  "exports": {
    ".": "./src/index.js"
  },
  "files": [
    "src"
  ],
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "format": "prettier --write \"src/**/*.js\" \"**/*.{js,cjs,md,json}\"",
    "format:check": "prettier --check \"src/**/*.js\" \"**/*.{js,cjs,md,json}\"",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage --coverage.provider=istanbul --coverage.reporter=text --coverage.reporter=lcov --coverage.include=\"src/**/*.js\"",
    "prepare": "if [ \"$npm_command\" = \"install\" ] && [ -z \"$CI\" ] && command -v husky >/dev/null 2>&1; then husky; fi",
    "security-audit": "audit-ci",
    "git:pre-commit-hook": "npm run security-audit && npm run format:check && npm run lint"
  },
  "author": "Defra DDTS",
  "license": "OGL-UK-3.0",
  "dependencies": {
    "@hapi/wreck": "18.1.2"
  },
  "devDependencies": {
    "@defra/lis-infra-eslint-config": "^0.6.0",
    "@types/node": "^26.2.0",
    "@vitest/coverage-istanbul": "^4.1.10",
    "audit-ci": "^7.1.0",
    "eslint": "^9.0.0",
    "husky": "^9.1.7",
    "prettier": "^3.9.6",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "eslint": "^9.0.0"
  }
}
